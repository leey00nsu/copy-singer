{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"/usr/local/bin/node\" -e \"(async () => {   const fs = require('node:fs');   const path = require('node:path');   const { pathToFileURL } = require('node:url');   const relativeHookPath = \\\".codex/hooks/session_start_lee_spec_kit.mjs\\\";   let dir = process.cwd();   while (true) {     const candidate = path.join(dir, relativeHookPath);     if (fs.existsSync(candidate)) {       await import(pathToFileURL(candidate).href);       return;     }     const parent = path.dirname(dir);     if (parent === dir) {       throw new Error('lee-spec-kit hook script not found: ' + relativeHookPath);     }     dir = parent;   } })().catch((error) => {   console.error(error && error.stack ? error.stack : String(error));   process.exit(1); });\"",
            "statusMessage": "Loading lee-spec-kit workflow context"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"/usr/local/bin/node\" -e \"(async () => {   const fs = require('node:fs');   const path = require('node:path');   const { pathToFileURL } = require('node:url');   const relativeHookPath = \\\".codex/hooks/user_prompt_submit_lee_spec_kit.mjs\\\";   let dir = process.cwd();   while (true) {     const candidate = path.join(dir, relativeHookPath);     if (fs.existsSync(candidate)) {       await import(pathToFileURL(candidate).href);       return;     }     const parent = path.dirname(dir);     if (parent === dir) {       throw new Error('lee-spec-kit hook script not found: ' + relativeHookPath);     }     dir = parent;   } })().catch((error) => {   console.error(error && error.stack ? error.stack : String(error));   process.exit(1); });\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"/usr/local/bin/node\" -e \"(async () => {   const fs = require('node:fs');   const path = require('node:path');   const { pathToFileURL } = require('node:url');   const relativeHookPath = \\\".codex/hooks/pre_tool_use_policy.mjs\\\";   let dir = process.cwd();   while (true) {     const candidate = path.join(dir, relativeHookPath);     if (fs.existsSync(candidate)) {       await import(pathToFileURL(candidate).href);       return;     }     const parent = path.dirname(dir);     if (parent === dir) {       throw new Error('lee-spec-kit hook script not found: ' + relativeHookPath);     }     dir = parent;   } })().catch((error) => {   console.error(error && error.stack ? error.stack : String(error));   process.exit(1); });\"",
            "statusMessage": "Checking lee-spec-kit workflow guardrails"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"/usr/local/bin/node\" -e \"(async () => {   const fs = require('node:fs');   const path = require('node:path');   const { pathToFileURL } = require('node:url');   const relativeHookPath = \\\".codex/hooks/stop_workflow_audit.mjs\\\";   let dir = process.cwd();   while (true) {     const candidate = path.join(dir, relativeHookPath);     if (fs.existsSync(candidate)) {       await import(pathToFileURL(candidate).href);       return;     }     const parent = path.dirname(dir);     if (parent === dir) {       throw new Error('lee-spec-kit hook script not found: ' + relativeHookPath);     }     dir = parent;   } })().catch((error) => {   console.error(error && error.stack ? error.stack : String(error));   process.exit(1); });\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
